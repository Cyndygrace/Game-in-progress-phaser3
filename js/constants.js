// This allows up to be sure that the message we are sending is the message we are listining for
class Constants {
  constructor() {
    this.SET_SCORE = 'setScore';
    // add points
    this.UP_POINTS = 'upPoints';
    this.SCORE_UPDATED = 'scoreUpdated';
    this.UP_MONEY = 'upMoney';
  }
}
